{
  "version": 2,
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/staticfiles/$1"
    },
    {
      "src": "/(.*)",
      "dest": "config.asgi.application"
    }
  ],
  "installCommand": "pip install -r requirements.txt"
}
